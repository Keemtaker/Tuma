<div class="container">
<div class="row">
<div class="col-sm-9">
<h1>Here are the available jobs</h1>

<%= search_form_for @search, :id => 'jobs_search' do |f| %>
  <%= f.label :salary_or_job_type_or_title_cont %>
  <%= f.search_field :salary_or_job_type_or_title_cont %>
  <%= f.submit "search" %>
<% end %>
  <div id="jobs"><%= render 'jobs' %></div>


</div>
</div>
</div>
